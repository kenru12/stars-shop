<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Stars Shop</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        :root {
            --bg-pure: #000000;
            --bg-card: #1c1c1e;
            --tg-button: #2481cc;
            --tg-hint: #888888;
            --accent-gradient: linear-gradient(135deg, #007aff, #00c6ff);
        }

        body {
            margin: 0; background-color: var(--bg-pure); color: #fff;
            font-family: -apple-system, system-ui, sans-serif;
            overflow: hidden; height: 100vh;
        }

        canvas { position: fixed; top: 0; left: 0; pointer-events: none; z-index: 0; opacity: 0.8; }
        
        .app { position: relative; z-index: 1; height: 100vh; display: flex; flex-direction: column; }
        
        /* --- ИСПРАВЛЕНИЕ: Увеличил padding-top с 20px до 85px --- */
        .content { 
            flex: 1; 
            overflow-y: auto; 
            padding: 16px; 
            padding-top: 85px; /* Теперь контент не будет перекрыт крестиком */
            padding-bottom: 100px; 
        }

        .promo-banner {
            background: linear-gradient(135deg, #1d2733 0%, #2481cc 100%);
            border-radius: 16px; padding: 20px; margin-bottom: 20px;
            display: flex; align-items: center; justify-content: space-between;
        }

        .input-group { 
            background: var(--bg-card); border-radius: 14px; 
            padding: 4px 12px; margin-bottom: 12px; 
            display: flex; align-items: center; min-height: 56px;
        }
        
        .avatar-slot { 
            width: 0; height: 34px; border-radius: 50%; 
            overflow: hidden; margin-right: 0; 
            opacity: 0; transform: scale(0.5);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1); 
        }
        .avatar-slot.visible { width: 34px; margin-right: 12px; opacity: 1; transform: scale(1); }
        .avatar-slot img { width: 100%; height: 100%; object-fit: cover; }

        input { flex: 1; background: transparent; border: none; color: #fff; font-size: 16px; outline: none; }
        .btn-self { background: var(--tg-button); color: #fff; border: none; border-radius: 18px; padding: 6px 14px; font-size: 13px; font-weight: 600; }

        .stars-grid { 
            display: flex; overflow-x: auto; gap: 10px; margin: 16px 0; padding-bottom: 5px;
        }
        .stars-grid::-webkit-scrollbar { display: none; }

        .star-card { 
            background: var(--bg-card); border-radius: 12px; padding: 10px 16px; 
            display: flex; align-items: center; gap: 6px; white-space: nowrap;
            border: 1.5px solid transparent; transition: 0.2s; flex-shrink: 0;
        }
        .star-card.active { border-color: #f9d43d; background: rgba(249, 212, 61, 0.1); }
        .star-card b { color: #f9d43d; font-size: 15px; }

        .label-txt { color: var(--tg-hint); font-size: 14px; margin: 15px 0 10px; display: block; font-weight: 500; }
        .pay-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 20px; }
        .pay-item { background: var(--bg-card); border-radius: 12px; padding: 16px; text-align: center; border: 1px solid transparent; font-weight: 600; }
        .pay-item.active { border-color: var(--tg-button); background: rgba(36, 129, 204, 0.1); }

        .main-btn { background: var(--tg-button); color: #fff; width: 100%; border: none; border-radius: 14px; padding: 18px; font-size: 16px; font-weight: 700; margin-top: 10px; cursor: pointer;}
        /* Вторая кнопка для вывода (зеленая или нейтральная) */
        .withdraw-btn { background: #34c759; margin-top: 15px; }

        .rules-link-container {
            text-align: center; font-size: 11px; color: #8e8e93; margin-top: 15px;
        }
        .rules-link-container a { color: #2481cc; text-decoration: none; border-bottom: 1px dotted #2481cc; cursor: pointer; }

        .modal {
            display: none; position: fixed; z-index: 9999; left: 0; top: 0; width: 100%; height: 100%;
            background-color: rgba(0,0,0,0.85); backdrop-filter: blur(5px);
        }
        .modal-content {
            background-color: #1c1c1e; margin: 20% auto; padding: 25px;
            border: 1px solid #333; width: 85%; border-radius: 16px;
            color: #fff; max-height: 70vh; overflow-y: auto; font-size: 13px; line-height: 1.5;
        }
        .close-btn { color: #aaa; float: right; font-size: 28px; font-weight: bold; cursor: pointer; }
        .modal h2 { margin-top: 0; font-size: 18px; color: var(--tg-button); }
        .modal p { margin-bottom: 10px; }

        .ref-cards-container { display: flex; justify-content: space-between; gap: 10px; margin-bottom: 20px; }
        .ref-card {
            background: var(--accent-gradient); border-radius: 16px; width: 48%; height: 120px;
            display: flex; flex-direction: column; align-items: center; justify-content: center;
            color: white; box-shadow: 0 4px 15px rgba(0, 122, 255, 0.25);
        }
        .duck-icon { width: 64px; height: 64px; object-fit: contain; }
        .percent-number { font-size: 32px; font-weight: 800; }
        .percent-text { font-size: 12px; opacity: 0.9; }
        
        .ref-link-box {
            background: var(--bg-card); padding: 5px; border-radius: 14px; display: flex; margin-bottom: 15px; border: 1px solid #333;
        }
        .ref-link-box input { padding: 10px; color: #888; font-size: 14px; }
        .ref-link-box button { background: none; border: none; font-size: 20px; cursor: pointer; padding: 0 10px; }

        .ref-stats { display: flex; justify-content: space-around; margin-top: 20px; padding-top: 20px; border-top: 1px solid #222; }
        .stat-item { text-align: center; }
        .stat-item span { display: block; font-size: 20px; font-weight: bold; color: #fff; }
        .stat-item small { color: #777; font-size: 11px; }

        .nav-bar { 
            position: fixed; bottom: 0; left: 0; right: 0; background: rgba(10,10,10,0.98); 
            display: flex; padding: 10px 0 25px; border-top: 1px solid #222; z-index: 10;
        }
        .nav-item { flex: 1; text-align: center; background: none; border: none; color: #555; font-size: 10px; padding: 0; }
        .nav-item div { font-size: 20px; margin-bottom: 4px; }
        .nav-item.active { color: var(--tg-button); }
        
        .tab { display: none; animation: fadein 0.2s; }
        .tab.active { display: block; }
        @keyframes fadein { from { opacity: 0; } to { opacity: 1; } }

        .placeholder-screen { text-align: center; padding-top: 50px; color: #777; }
        .placeholder-icon { font-size: 60px; margin-bottom: 20px; display: block; opacity: 0.3; }
    </style>
</head>
<body>
    <canvas id="stars"></canvas>
    
    <div class="app">
        <div class="content">
            
            <div id="tab-shop" class="tab active">
                <div class="promo-banner">
                    <div style="color:white"><b>Telegram Stars</b><br><span style="font-size:12px;opacity:0.8">Мгновенная отправка</span></div>
                    <div style="font-size:30px">⭐</div>
                </div>
                
                <div class="input-group">
                    <div id="avatar-container" class="avatar-slot"><img id="dynamic-ava" src=""></div>
                    <input type="text" id="target-user" placeholder="@username" oninput="resolveUser()">
                    <button class="btn-self" onclick="setToMe()">Себе</button>
                </div>

                <div class="input-group">
                    <input type="number" id="manual-count" placeholder="Введите количество звёзд" oninput="updatePriceManual()">
                </div>

                <div class="stars-grid">
                    <div class="star-card" onclick="selectPack(50, this)"><b>50 ★</b></div>
                    <div class="star-card" onclick="selectPack(100, this)"><b>100 ★</b></div>
                    <div class="star-card" onclick="selectPack(250, this)"><b>250 ★</b></div>
                    <div class="star-card" onclick="selectPack(500, this)"><b>500 ★</b></div>
                    <div class="star-card" onclick="selectPack(1000, this)"><b>1000 ★</b></div>
                </div>

                <span class="label-txt">Оплата</span>
                <div class="pay-grid">
                    <div class="pay-item active" onclick="selectPay('sbp', this)">СБП</div>
                    <div class="pay-item" onclick="selectPay('card', this)">Карты (RUB)</div>
                </div>

                <button class="main-btn" id="pay-button" onclick="handleBuy()">Купить</button>
                
                <div class="rules-link-container">
                    Покупая «Telegram Stars», вы соглашаетесь с <a onclick="openRules()">правилами сервиса</a>.
                </div>
            </div>

            <div id="tab-sell" class="tab">
                <div class="placeholder-screen">
                    <span class="placeholder-icon">📉</span>
                    Скоро вы сможете продавать свои Stars здесь.
                </div>
            </div>

            <div id="tab-ref" class="tab">
                <h2 style="margin-top:0">Реферальная программа</h2>
                <div class="ref-cards-container">
                    <div class="ref-card">
                        <img src="https://img.icons8.com/color/96/rubber-duck.png" alt="Duck" class="duck-icon">
                    </div>
                    <div class="ref-card">
                        <div class="percent-number">4%</div>
                        <div class="percent-text">Твой процент</div>
                    </div>
                </div>

                <div class="ref-link-box">
                    <input type="text" id="ref-link" readonly value="Загрузка...">
                    <button onclick="copyRef()">📋</button>
                </div>

                <button class="main-btn" onclick="shareRef()">Пригласить друга</button>

                <div class="ref-stats">
                    <div class="stat-item">
                        <span id="ref-count">...</span>
                        <small>Приглашено</small>
                    </div>
                    <div class="stat-item">
                        <span id="ref-earnings">... ₽</span>
                        <small>Баланс</small>
                    </div>
                </div>
                
                <button class="main-btn withdraw-btn" onclick="requestWithdraw()">💸 Вывести средства</button>
                <p style="text-align:center; font-size:11px; color:#666; margin-top:10px;">
                    Вывод от 100 ₽ на карту или СБП
                </p>
            </div>

            <div id="tab-check" class="tab">
                <div class="placeholder-screen">
                    <span class="placeholder-icon">🧾</span>
                    Проверка транзакций
                </div>
            </div>

            <div id="tab-history" class="tab">
                <h2 style="margin-top:0">История</h2>
                <div style="background:var(--bg-card); padding:20px; border-radius:12px; text-align:center; color:#777;">
                    Пока пусто
                </div>
            </div>

        </div>

        <div class="nav-bar">
            <button class="nav-item active" onclick="switchTab('tab-shop', this)"><div>⭐</div>Звезды</button>
            <button class="nav-item" onclick="switchTab('tab-sell', this)"><div>📉</div>Продать</button>
            <button class="nav-item" onclick="switchTab('tab-ref', this)"><div>👥</div>Рефка</button>
            <button class="nav-item" onclick="switchTab('tab-check', this)"><div>🧾</div>Чек</button>
            <button class="nav-item" onclick="switchTab('tab-history', this)"><div>📋</div>История</button>
        </div>
    </div>

    <div id="rulesModal" class="modal">
        <div class="modal-content">
            <span class="close-btn" onclick="closeRules()">&times;</span>
            <h2>Правила DuckStars</h2>
            <div id="rules-text">
                 <p>1. Общие положения...</p>
                 <p>2.2. Вознаграждение 4%...</p>
                 <p>Контакты: @starducksup</p>
            </div>
        </div>
    </div>

    <script>
        const tg = window.Telegram.WebApp;
        tg.expand();
        tg.headerColor = '#000000';
        
        // --- ДАННЫЕ ЮЗЕРА ---
        const me = tg.initDataUnsafe.user || { id: 0, first_name: 'Guest', username: '', photo_url: '' };
        
        // ВНИМАНИЕ: Эти данные в идеале должны приходить от твоего бота при открытии приложения.
        // Пока мы ставим заглушку, но ниже я опишу как это сделать правильно.
        let userBalance = 0; // Сюда подставится баланс
        
        document.addEventListener('DOMContentLoaded', () => {
            const avaImg = document.getElementById('dynamic-ava');
            const avaSlot = document.getElementById('avatar-container');
            
            if (me.photo_url) {
                avaImg.src = me.photo_url;
                avaSlot.classList.add('visible');
            } else if (me.first_name) {
                avaImg.src = `https://ui-avatars.com/api/?name=${me.first_name}&background=2481cc&color=fff`;
                avaSlot.classList.add('visible');
            }

            if (me.id) {
                document.getElementById('ref-link').value = `https://t.me/DuckStarsBot?start=${me.id}`;
            }

            // Имитация загрузки баланса (в реальности бот должен передавать это)
            document.getElementById('ref-earnings').innerText = userBalance + " ₽";
        });

        // --- ЛОГИКА ВКЛАДОК ---
        function switchTab(id, el) { 
            document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
            document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
            document.getElementById(id).classList.add('active'); 
            el.classList.add('active');
        }

        // --- ПОИСК ЮЗЕРА ---
        let searchTimer;
        async function resolveUser() {
            const input = document.getElementById('target-user').value.replace('@', '').trim();
            const slot = document.getElementById('avatar-container');
            const img = document.getElementById('dynamic-ava');
            
            if(input.length < 3) { 
                if(me.photo_url) { img.src = me.photo_url; slot.classList.add('visible'); }
                else { slot.classList.remove('visible'); }
                return; 
            }
            clearTimeout(searchTimer);
            searchTimer = setTimeout(async () => {
                img.src = `https://ui-avatars.com/api/?name=${input}&background=2481cc&color=fff`;
                slot.classList.add('visible');
            }, 500);
        }
        function setToMe() { document.getElementById('target-user').value = me.username || ''; resolveUser(); }

        // --- ЛОГИКА ПОКУПКИ ---
        const RATE = 1.54;
        let currentPayMethod = 'sbp';

        function selectPack(val, el) {
            document.querySelectorAll('.star-card').forEach(c => c.classList.remove('active'));
            el.classList.add('active');
            document.getElementById('manual-count').value = val;
            updatePriceBtn(val);
        }

        function updatePriceManual() {
            document.querySelectorAll('.star-card').forEach(c => c.classList.remove('active'));
            const val = document.getElementById('manual-count').value;
            updatePriceBtn(val);
        }

        function updatePriceBtn(val) {
            document.getElementById('pay-button').innerText = val > 0 ? `Купить за ${Math.ceil(val * RATE)} ₽` : 'Купить';
        }

        function selectPay(method, el) {
            document.querySelectorAll('.pay-item').forEach(c => c.classList.remove('active'));
            el.classList.add('active');
            currentPayMethod = method;
        }

        function handleBuy() {
            const v = document.getElementById('manual-count').value;
            const to = document.getElementById('target-user').value;
            if(!v) return tg.showAlert("Введите количество звезд");
            tg.sendData(JSON.stringify({action: 'buy', stars: v, to: to || me.username, method: currentPayMethod}));
        }

        // --- ЛОГИКА ВЫВОДА СРЕДСТВ ---
        function requestWithdraw() {
            // В реальности тут можно проверять > 100 рублей
            tg.showConfirm("Отправить заявку на вывод средств поддержке?", (status) => {
                if (status) {
                    // Отправляем данные боту
                    tg.sendData(JSON.stringify({
                        action: 'withdraw',
                        user_id: me.id
                    }));
                }
            });
        }

        // --- УТИЛИТЫ ---
        function openRules() { document.getElementById('rulesModal').style.display = "block"; }
        function closeRules() { document.getElementById('rulesModal').style.display = "none"; }
        
        function copyRef() {
            const copyText = document.getElementById("ref-link");
            copyText.select(); navigator.clipboard.writeText(copyText.value).then(() => { tg.showAlert("Скопировано!"); });
        }
        function shareRef() {
            const url = `https://t.me/share/url?url=${encodeURIComponent(document.getElementById("ref-link").value)}`;
            tg.openTelegramLink(url);
        }

        // --- ЗВЕЗДНЫЙ ФОН ---
        const canvas = document.getElementById('stars'); const ctx = canvas.getContext('2d');
        let w, h, dots = [];
        function init() { w = canvas.width = window.innerWidth; h = canvas.height = window.innerHeight; dots = Array.from({length:100},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*2+0.5,s:Math.random()*0.6+0.2})); }
        function draw() { ctx.clearRect(0,0,w,h); ctx.fillStyle="#fff"; dots.forEach(d=>{ctx.beginPath();ctx.arc(d.x,d.y,d.r,0,Math.PI*2);ctx.fill();d.y+=d.s;if(d.y>h)d.y=-5;}); requestAnimationFrame(draw); }
        init(); draw(); window.onresize=init;
    </script>
</body>
</html>